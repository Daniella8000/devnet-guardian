[project]
name = "devnet-guardian"
authors = ["DevNet Labs"]
description = "Decentralized health record management platform with sovereign data control"
telemetry = false
requirements = []
[contracts.health-guardian]
path = "contracts/health-guardian.clar"
depends_on = []

[repl]
costs_version = 2
parser_version = 2

[repl.analysis]
passes = ["check_checker"]

[repl.analysis.check_checker]
strict = false
trusted_sender = false
trusted_caller = false
callee_filter = false
